schema_version: 1

data:
  name: parallel_corpus
  source_language: en
  target_language: fr
  train_path: data/raw/train.parquet
  validation_path: data/raw/val.parquet
  test_path: data/raw/test.parquet
  max_sequence_length: 128
  batch_size: 64
  num_workers: 0
